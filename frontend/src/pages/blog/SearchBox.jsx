// NODE_MODULES
import { makeStyles } from "@mui/styles"

const useStyles = makeStyles({

})

const SearchBox = () => {
  const classes = useStyles();
  return (
    <div className={classes.aside}>SearchBox</div>
  )
}

export default SearchBox